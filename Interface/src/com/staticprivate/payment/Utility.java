package com.staticprivate.payment;

public class Utility
{
// before java 8 when it introduces the that we can write our logic of same logic into the static method which is spread through the multiple classes.
	//but in java 8 we can write our program( define static method) inside the interface.
	//in java 9 we can write a private method inside the interface as well so that we can remain safe to writing same logic inside our database to fetching the same info for different static methods.
}
